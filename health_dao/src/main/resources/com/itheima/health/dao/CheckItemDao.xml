<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itheima.health.dao.CheckItemDao">
    <!--新增-->
    <insert id="add" parameterType="com.itheima.health.pojo.CheckItem">
        insert into t_checkitem(code,name,sex,age,price,type,remark,attention)
        values
        (#{code},#{name},#{sex},#{age},#{price},#{type},#{remark},#{attention})
    </insert>


    <!--编辑-->
    <update id="edit" parameterType="com.itheima.health.pojo.CheckItem">
        UPDATE t_checkitem
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="code != null">
                code = #{code},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="attention != null">
                attention = #{attention},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
        </set>
       where id = #{id}
    </update>

    <!--删除-->
    <delete id="deleteById" parameterType="int">
        DELETE FROM t_checkitem WHERE id = #{id}
    </delete>


    <!--分页 条件查询-->
    <select id="selectByCondition" parameterType="string" resultType="com.itheima.health.pojo.CheckItem">
        SELECT * FROM t_checkitem
        <if test="value != null and value.length>0">
            WHERE code = #{value} or name = #{value}
        </if>
    </select>

    <!--根据检查项id查询中间关系表-->
    <select id="findCountCheckItemId" resultType="java.lang.Long" parameterType="int">
        SELECT COUNT(*) FROM t_checkgroup_checkitem WHERE checkitem_id = #{checkitem_id}
    </select>



    <!--根据检查项id查询检查项信息-->
    <select id="findCheckItemId" resultType="com.itheima.health.pojo.CheckItem" parameterType="int">
        SELECT * FROM t_checkitem WHERE id = #{id}
    </select>



    <!--查询所有-->
    <select id="findAll" resultType="com.itheima.health.pojo.CheckItem">
        SELECT * FROM t_checkitem
    </select>


    <!--根据检查组id查询检查项信息-->
    <select id="findCheckItemListById" resultType="com.itheima.health.pojo.CheckItem">
        select * from t_checkitem  where id
    in (select checkitem_id from t_checkgroup_checkitem where checkgroup_id=#{id})
    </select>
</mapper>